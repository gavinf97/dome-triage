"""Classifier implementations: baseline TF-IDF+LogReg (Phase 3), Bioformer/PubMedBERT fine-tune
(Phase 4), and the LLM backend bake-off (Phase 5). Not yet implemented -- see ROADMAP.md for the
shared `Classifier.predict(title, abstract, metadata) -> Prediction` protocol every model here
must implement."""
